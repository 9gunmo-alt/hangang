# 매장 노트북 실시간 봇 설치

노트북이 켜져 있는 동안 1분마다 5대 매출을 긁어 GitHub에 올린다.
폰 대시보드는 이걸 바로 받아 보여줘서 "거의 실시간"이 된다.

## 처음 한 번만
1. 이 저장소는 이미 GitHub Desktop으로 노트북에 받아져 있음(그 폴더 사용).
2. 파이썬이 없으면 python.org 에서 설치 — 설치 화면에서 **"Add python.exe to PATH" 체크** 필수.
3. 저장소 폴더에서 **`setup.bat` 더블클릭** → 라이브러리 + 브라우저 자동 설치.

## 매일(또는 계속)
- 저장소 폴더에서 **`run.bat` 더블클릭** → 검은 창이 뜨고 1분마다 수집.
- **이 창을 켜두면** 계속 돎. 노트북 꺼지거나 창 닫으면 멈춤(다시 run.bat 하면 재개).
- 매출이 바뀔 때만 올리므로, 조용한 시간엔 "변화 없음"만 뜨는 게 정상.

## 참고
- 폰 대시보드는 raw 주소에서 최신을 읽음(즉시 반영). 화면 볼 때마다/3분마다 자동 새로고침 + ↻ 버튼.
- 노트북이 꺼져 있을 땐 GitHub Actions에서 **매출 수집 → Run workflow** 로 수동 갱신 가능.
- 봇이 첫 실행 때 묶음거래 상세를 캐시(`bundle_cache.json`)에 저장 → 이후 사이클은 가벼움.
